"""Customer-support A2A agents.

Each module exports `CARD` and `Executor`. These agent files are pure
A2A-protocol code -- they do not know whether their orchestrator is the
hand-rolled supervisor or agent2society. The same files are used by both.
"""
